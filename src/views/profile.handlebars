<div class="container mt-5">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <h2>tu perfil</h2>
           
              <p>{{user.nombre}}</p>
              <p>{{user.email}} </p>
              <p>{{user.edad}} </p>
        </div>
    </div>
</div>

